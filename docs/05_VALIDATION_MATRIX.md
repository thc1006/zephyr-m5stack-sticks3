# Validation Matrix

Last hardware run: 2026-08-10 on the 3rd physical M5Stack StickS3
(ESP32-S3-PICO-1, MAC 98:88:e0:0e:dc:64), Zephyr build `697b32395414`, SDK 1.0.1.
Earlier rows were taken on the first unit (MAC 70:04:1d:db:ab:b8), Zephyr 4.4.0;
each row names the unit it was measured on.

| Area | Status | Evidence | Notes |
|---|---|---|---|
| Prior-art search | Done (2026-05-30) | `docs/00_RESEARCH_SNAPSHOT.md` | No upstream/community Zephyr StickS3 board found — first public port |
| Repo integrity | Passing | `bash verify.sh` | Always runnable |
| Driver unit tests (M5PM1 MFD path) | **Verified** | `tests/drivers/m5pm1_mfd` (native_sim) | I2C emulator + ztest on native_sim, 4/4 pass: idle-sleep disable (reg 0x09), wake-retry, PYG2/L3B enable sequence, VBAT ADC read. Covers the on-board MFD path; load-bearing seeds (dirty regs + first-transfer fault). Replaces the deleted interim-regulator ztest. Logic-verified, not an independent silicon oracle |
| Driver unit tests (ES8311 codec) | **Verified** | `tests/drivers/audio/es8311` (native_sim) | I2C emulator + ztest on native_sim, **72/72 pass** (was 11, then 29, grown across the 2026-07 adversarial reviews): chip-ID (+ a foreign chip id rejected with -ENODEV), 16 kHz/16-bit playback + capture (ADC) configure + write order, volume/mute, unsupported route/format/property rejection, I2C-error propagation, and the new coverage: **every supported sample rate emits an identical clock-register set** (the ratiometric 256fs claim, made falsifiable), rejected rates / 24-bit / 32-bit words / non-256fs MCLK, the INPUT_VOLUME and INPUT_MUTE round trip (mute via the ADC serial-port bit 0x0A[6], not the volume floor), volume clamping at both ends, `start_output()`/`stop_output()`, and mute state surviving a reconfigure. Later reviews added: the endpoint-relative I2S clock-role contract (accept `TARGET\|TARGET`, reject `CONTROLLER`, matching `samples/drivers/i2s/i2s_codec`), the three board-policy devicetree properties (`everest,mono-dac-source` / `mic-pga-gain-db` / `output-mode`), the split output lifecycle, best-effort re-mute on a failed apply, and a reconfigure of a *live* route. Logic-verified |
| Capture DSP unit tests (audio_dsp) | **Verified** | `tests/drivers/audio_dsp` (native_sim) | Pure-logic ztest: isqrt, RMS (64-bit accum, no INT16_MIN overflow), peak (INT16_MIN clamp), I2S slot deinterleave (slot-index guard), level-to-bars mapping. Logic-verified; the same DSP read rms=4089/peak=5800 from the HW digital loopback |
| Zephyr workspace bootstrap | Done | host: Zephyr 4.4.0 + SDK 1.0.1 (WSL2 Ubuntu) | `scripts/bootstrap_zephyr_ubuntu.sh` |
| Board target discovery | **Verified** | `west boards` lists `m5stack_sticks3` | HWMv2 layout, `esp32s3/procpu` + `esp32s3/appcpu` |
| Build validation | **Verified** | `evidence/20260530-0345-build.log` | `west build -b m5stack_sticks3/esp32s3/procpu app`; FLASH 1.64% |
| Flash validation | **Verified** | `evidence/20260530-0432-flash.log` | esptool `write-flash 0x0` `--after watchdog-reset`; hash verified |
| Boot console | **Verified PASS (HW-001)** | `evidence/20260530-0432-serial.log` | USB-Serial/JTAG console; stable uptime, no reset loop |
| Buttons G11/G12 | **Verified PASS (HW-002)** | `evidence/20260530-buttons.log`, `evidence/20260530-b34.log` | gpio-keys input events confirmed for BOTH keys (`code=11` + `code=2`). Physical map (facing device): MIDDLE=KEY1/G11, RIGHT=KEY2/G12, LEFT=power (PMIC, reboots) |
| Regulator disable (HW) | **Verified (B-3)** | `evidence/20260530-b34.log` | `regulator_disable`/`enable` blinks the backlight off/on on hardware (user-confirmed) |
| BMI270 IMU | **Verified PASS (HW-004)** | `evidence/20260530-imu.log` | I2C0 G47/G48, BMI270@0x68; live accel tracks gravity across X/Y/Z on rotation. INT is behind M5PM1 (no host int-gpios) |
| M5PM1 PMIC (LCD rail) | **Verified** | `evidence/20260601-hw010-p3-battery-boot.log` | L3B rail is a `regulator-fixed` switched via the M5PM1 **MFD** gpio child (PYG2) — vendored #109961 MFD/GPIO/ADC + a local idle-sleep/wake-retry fix. Powers the LCD on hardware (HW-010). The interim single-rail `m5stack,m5pm1-l3b-regulator` driver was superseded and removed |
| LCD ST7789P3 | **Verified PASS (HW-003)** | `evidence/20260530-lcd.log` + photos `evidence/PXL_20260529_2351*.jpg` | mipi-dbi-spi + sitronix,st7789v, x/y offset 52/40, inversion ON; panel cycles R/G/B/W (photos show green + red frames), `disp=ready` |
| M5PM1 power (full) | Partial | `evidence/20260601-hw010-p3-battery-boot.log` | Via the MFD: GPIO child (L3B/PYG2 rail + PYG3 audio-amp gate) and ADC child (VBAT, HW-010) done. **The M5PM1 silicon exposes voltages only — no battery/charge current, coulomb counter or SoC register** (verified against the official register map + #109961 + Espressif's driver), so on-device current / per-state current (issue #4) is not obtainable; only charge-enable + power-source/insertion status remain as real "full PMIC" additions |
| IR TX (NEC) | **Verified PASS (HW-005)** | `evidence/20260601-ir-hw-session.md` | TX via the LEDC ~38 kHz carrier (G46); emits NEC frames on hardware (serial `ir_tx` + phone camera), LCD stays lit. NEC encode/decode native_sim ztest 9/9. Gated `CONFIG_APP_IR` (no blob) |
| IR RX (NEC) | **Verified PASS (HW-005)** | `evidence/20260601-ir-rx-edges.log` | GPIO edge interrupt on G42 (MCPWM capture drops edges on the fast NEC burst) + NEC decode. Receiver confirmed: a real remote made ~10k edges in 40 s; NEC decoded via on-device TX→RX loopback. Non-NEC remotes still register on the "IR act" counter; other protocol decoders are out of scope (no Zephyr IR subsystem). Gated `CONFIG_APP_IR` |
| ES8311 audio | **Verified (P5)** | see "ES8311 audio (P5, v0.6)" row below | Codec 0x18 + I2S; gated `CONFIG_APP_AUDIO` (out of scope for the default/v0.1 build) |
| Demo skeleton (P0, v0.6) | **Verified PASS (HW-007)** | `evidence/20260531-hw007-p0-boot.log` + user-confirmed nav | Modular app (main/ui/status/pages) + atomic page index + instant (semaphore-woken) button nav; KEY1 next / KEY2 prev cycling HOME/IMU/DIAG. Button input events themselves logged in HW-002 |
| Text UI (P1, v0.6) | **Verified PASS (HW-008)** | `evidence/20260531-hw008-p1-text-boot.log` + user-confirmed | RGB565 font blitter (vendored cfb_font_1016) renders legible upright text; PAGE_HOME shows title + live uptime; byte order auto-detected RGB_565 |
| Live pages (P2, v0.6) | **Verified PASS (HW-009)** | `evidence/20260531-hw009-p2-pages-boot.log` + user-confirmed | 4 pages (HOME/IMU/POWER/DIAG) + page-name header; live IMU accel xyz with correct signs; flicker-free (clear only on page change); KEY1 next / KEY2 prev |
| Battery / M5PM1 MFD+ADC (P3, v0.6) | **Verified PASS (HW-010)** | `evidence/20260601-hw010-p3-battery-boot.log` + user-confirmed VBAT 4182 mV | M5PM1 restructured to vendored #109961 MFD + ADC + GPIO; L3B rail now `regulator-fixed` on the gpio child; VBAT via Zephyr ADC API (4182 mV, matches issue #4); LCD still lit through the new path; MFD adds idle-sleep + wake-retry (local delta vs #109961) |
| Battery SoC% + power source (HW-017) | **Verified PASS (HW-017, headless)** | `evidence/20260605-hw017-battery-soc.log` | Issue #8. Voltage-only OCV fuel gauge: upstream `zephyr,fuel-gauge-composite` over VBAT via a unity `voltage-divider` (LiPo `BATTERY_OCV_CURVE_LITHIUM_ION_POLYMER_DEFAULT`). Serial `bat=4170 soc=100 vin=5142`: SoC reads a real 100% (flat top, VBAT > 4.032 V curve top, as documented) and VIN ~5.14 V maps to "USB 5V". No driver written (stock bindings). `tests/drivers/battery` native_sim 4/4 covers the bar/source mapping; OCV interpolation is upstream-tested. Approximate (voltage-only, no coulomb counter, no load comp); "charging" out of scope (no reliable M5PM1 charge-status). LCD photo `evidence/PXL_20260605_070039537.MP.jpg` shows PAGE_POWER (VBAT 4172 mV / SoC 100% / src USB 5V); the SoC bar overflows the 135 px line and is clipped (cosmetic) |
| BLE advert + GATT (P4, v0.6) | **Verified PASS (HW-011/012)** | `evidence/20260601-hw011-012-p4-ble.md` (PC bleak) | Connectable adv "StickS3" + decoded manufacturer telemetry (uptime/bat/accel); custom GATT service read + ~1 Hz notify; adv-restart-after-disconnect bug found on HW and fixed (k_work-deferred bt_le_adv_start). Gated `CONFIG_APP_BLE`; default build blob-free |
| ES8311 audio (P5, v0.6) | **Verified PASS (HW-006/013)** | `evidence/20260601-hw006-p5-audio-boot.log` + user-confirmed beep | In-repo ES8311 codec driver (Zephyr audio codec API, native_sim ztest 72/72) + I2S + AW8737 amp via MFD gpio child; 440 Hz beep audible on the AUDIO page; LCD stayed lit when the amp toggled (masked write preserves L3B). Gated `CONFIG_APP_AUDIO`. Also the upstream task #21 driver |
| ES8311 audio capture / mic + live meter (HW-016d/e) | **Verified PASS (HW-016d/e)** | `evidence/20260606-hw016d-mic-works.log` (rms 0→30797 on a clap) + `evidence/PXL_20260606_032212870.MP~2.jpg` (on-screen meter `[####]` rms=20680); prior `evidence/20260605-hw016-audio-digital-loopback.log` | Issue #6 RESOLVED. A live mic capture (full-duplex, amp OFF, no beep) plus a loud external clap drove RMS from ~0-6 (quiet) to 12,000-30,000 with peak at full-scale 32767: the full analog chain — on-board MEMS mic → ES8311 ADC → I2S0 RX → DSP — captures real acoustic audio. The earlier `0x44 = 0x08` "rms=0 during the beep" was a WEAK-ACOUSTIC-STIMULUS confound (the tiny on-board 440 Hz speaker barely couples to the adjacent mic), not a mic/ADC fault: every ADC register reads back exactly as written and matches the known-good M5 EchoBase init, and the `0x44 = 0x68` digital-mux loopback (rms=4089/peak=5800) had already verified ASDOUT + I2S0 RX + DSP. PAGE_AUDIO is a live on-screen mic meter — HW-016e: speaking drove the on-device bar to `[####]` with `rms=20680`, clean header, device responsive (alive on the AUDIO page at the faster tick, no freeze observed in testing). The meter runs a dedicated capture thread that streams continuously while the page is up and only publishes the level; the UI reads it and never touches I2S (an inline per-render capture corrupted the next SPI write and wedged I2S). native_sim: es8311 72/72 + audio_dsp 8/8. Gated `CONFIG_APP_AUDIO` |
| Wi-Fi station logic (unit) | **Verified** | `tests/drivers/wifi` (native_sim) | 52/52 pass: config validate (SSID/PSK/SAE lengths, open, unknown), security/RSSI/bars, scan dedupe-by-BSSID + RSSI sort, connection FSM (backoff/cap/overflow/give-up/reset), AP SSID clamp + short-MAC zero-fill. Logic-verified; the glue is HW-verified |
| Wi-Fi scan (HW) | **Verified PASS (HW-014)** | `evidence/20260602-wifi-h2-scan.log` | ESP32-S3 native Wi-Fi (gated `CONFIG_APP_WIFI`, WPA2 build); `NET_REQUEST_WIFI_SCAN` → `scan done status=0 aps=24`, BSSID-deduped + RSSI-sorted, security/bars mapped. SPIRAM off; bundled ESP-IDF supplicant (not hostap) |
| Wi-Fi connect + DHCP (HW) | **Verified PASS (HW-015)** | `evidence/20260602-wifi-h3-connect.log` | Associated to a WPA2-PSK AP and obtained a DHCP lease (`status=0` CONNECTED, IPv4 192.168.50.49), stable; background scan suppressed while connected (a page entry can still scan once). Supplicant owns reconnect, `ESP32_WIFI_STA_AUTO_DHCPV4` owns DHCP. Credentials kept local/untracked, SSID-only logging. Gated `CONFIG_APP_WIFI`; mutually exclusive with BLE |
| ES8311 sample-rate sweep 8 kHz - 48 kHz + route transitions (HW-019) | **Verified PASS (HW-019)** | `evidence/20260712-hw019-es8311-rate-sweep-PASS.log` (9/9 rates PASS, 3/3 routes PASS, restore measured; 3rd physical StickS3, COM35, MAC 98:88:e0:0e:dc:64) + `evidence/20260712-hw019-i2s-slab-leak-diagnostic.log` + `evidence/20260712-hw019a-rate-sweep-attempt1-test-flaw.log` | **MEASURED, all nine rates.** The frame clock is timed against the kernel cycle counter (which is not derived from the I2S clock) and lands within 5 Hz of target everywhere: 8000/7999, 11025/11024, 12000/11999, 16000/15999, 22050/22049, 24000/23998, 32000/31998, 44100/44096, 48000/47996. The clock registers read back identical at every rate and the ADC is alive at every rate, so the 256fs argument (master clock from BCLK => 256 * Fs at every rate => one register set serves them all) is now hardware-verified end to end rather than argued from the datasheet. That argument is corroborated by the ES8311 user guide's own clock table, by Linux `sound/soc/codecs/es8311.c` (identical REG02 = 0x18 / REG05 = 0x00 at every rate) and by `i2s_esp32.c` producing BCLK = 32 * Fs exactly, and it is pinned by the ztest. `CONFIG_APP_AUDIO_RATE_SWEEP` reprograms I2S and the codec at each rate, reads the clock registers back over I2C and plays a tone while capturing it on the microphone. **8 kHz WORKS** (measured 7999 Hz, ADC alive). Both Espressif reference drivers special-case it (x4 instead of x8) claiming BCLK must be at least 512 kHz; the datasheet does not state it, Linux contradicts it, and Espressif's own 11.025 kHz row (x8 at 352.8 kHz) contradicts it. On this silicon the special case is not needed and `8000` stays in `es8311_rates[]`. **This run also found a real bug in the Zephyr ESP32 I2S driver** (not in the codec): `i2s_esp32_{rx,tx}_stop_transfer()` NULL the block the DMA is working on without returning it to the mem-slab, so every START/DROP cycle leaks one block per direction, and `i2s_buf_write()` allocates with `K_FOREVER` - so an exhausted slab is not an error, it is an unkillable block with no log line. Measured: tx/rx free counts fell 8,7,6,5,4,3,2,1 across the rates and the sweep hung silently at 44.1 kHz. **And the obvious fix for it is wrong** - see the HW-021 row below, which is the whole story. `scripts/patch_zephyr_i2s_leak.sh` + `scripts/patch_zephyr_dma_quiesce.sh` are the fix that actually works (the root cause is in `dma_stop()`, not in I2S - see HW-027); this sweep was RE-RUN against it (the first PASS was taken against the naive fix, which corrupts the mem-slab free list; it did not fire during that run, but evidence collected on a driver that is writing into freed memory is not evidence worth having). **What HW-019 will and will not prove.** Per rate it checks three independent things: the clock registers read back off the chip (which proves only that the driver wrote what it meant to); the frame clock **measured** by timing DMA blocks against the kernel cycle counter, which is not derived from the I2S clock (a mis-programmed divider shows up here and nowhere else); and that the ADC is alive with the amplifier off. **Read that third check with care, and see the HW-023 row: a zero noise floor does NOT mean a dead clock tree.** It far more often means "less than ~6 s since the codec's analog was disturbed", and this check cannot tell the two apart — which is exactly how the (false) claim that the software reset deafens the ADC below 22 kHz was manufactured out of this very sweep. It is only meaningful once the analog has settled, which on a warm-booted board it did minutes ago. **The ascending rate order is likewise not a free choice:** rate and time-since-reset are collinear in it. The acoustic loopback deliberately does NOT gate the verdict: TX and RX share the clock, so a common error cancels, and this speaker couples too weakly into this microphone for a threshold to mean anything. A green sweep is therefore **register + interface-timing + ADC-liveness** validation - not a proof of the codec's internal filters, OSR or analog bandwidth, which would need an external tone into the microphone and a measured recovered frequency. It also covers the **route transitions** on silicon (playback-only / capture-only / both, reading back 0x01/0x09/0x0A/0x0D/0x0E/0x12/0x14), because the rate loop only ever asks for PLAYBACK_CAPTURE and so writes not one of the power-down values the route fix is actually made of; until this runs they have only been checked against an emulator that cannot disagree with them. And it verifies that the sweep **restores** 16 kHz and hands back a working device, measured rather than assumed: the first version restored by calling `audio_init()`, which re-probes the codec, and `device_init()` returns `-EALREADY` on an already-initialized device - so it would have printed PASS for every rate and left audio dead |
| Shipping demo: record -> playback + the mic meter, after the driver rewrite (HW-022) | **Verified PASS (HW-022)** | `evidence/20260712-hw022-demo-record-playback-PASS.log` (user-driven on the REC page: `REC done: 448 ms peak_rms=5210` -> `PLAY done`, then a second clip `2560 ms peak_rms=4343` -> `PLAY done`; user confirmed the playback by ear) | **This row exists because of a hole I opened myself.** HW-019 and HW-021 flashed the sweep and the stress images; the app's own audio paths (beep, live mic meter, record, playback) are only reached by navigating to the REC page with the buttons, so **none of them had run since the ES8311 rewrite** - and `audio_slab_ok()`, the guard inserted into all four, had **never executed on hardware at all**. Invert its condition and audio would be silently dead while 29 ztests, CI 13/13, HW-019, HW-020 and HW-021 all stayed green. It ran, it passed, zero refusals. **Two things not dressed up:** (a) the device rebooted once mid-capture, with NO fault signature (a Zephyr fatal error prints `ZEPHYR FATAL ERROR` + `Halting system`; neither is there) but WITH the monitor's `ClearCommError (PermissionError)` / `Waiting for the device to reconnect` - i.e. the USB device vanished, which is what the LEFT power key does. Cannot prove which; record -> playback worked before it and again after. (b) `restarts=1` on every recording is **not a regression** - the original #14 verification reads `restarts=1` too. Its cause was a break in `do_record()` that logged nothing, so a session that died and restarted looked like one that just worked. In `main` since `c84e8d3`; the silence is now fixed |
| Zephyr ESP32 I2S mem-slab leak + the fix for it (HW-021) | **Verified PASS (HW-021)** | `evidence/20260712-hw021-i2s-slab-stock.log` (the leak: 8,7,6,5,4,3,2,1 then a silent hang), `evidence/20260712-hw021-i2s-crash-on-naive-fix.log` (the obvious fix CRASHES at cycle 2), `evidence/20260712-hw021-i2s-slab-quiesce-PASS.log` (the correct fix: 100/100 cycles, slab flat, canary clean) | An upstream Zephyr bug we found on hardware and fixed (zephyr issue #113310, PR #113311). `i2s_esp32_{rx,tx}_stop_transfer()` drop the DMA's in-flight mem-slab block without returning it, so every START/DROP leaks one block per direction, and `i2s_buf_write()` allocates with `K_FOREVER` - an exhausted slab is a **silent unkillable hang**, no error, no log line. **And the obvious fix is wrong**: hand the block back and the free list is corrupted by the DMA. A canary measures it: 1 of 8 free RX blocks written to per cycle, every cycle. The leak was masking a use-after-free. **The root cause turned out to be one layer down, in `dma_stop()` itself - see the HW-027 row.** `CONFIG_APP_I2S_STRESS` is the test; the fix is two independent patches, `scripts/patch_zephyr_dma_quiesce.sh` and `scripts/patch_zephyr_i2s_leak.sh`, which together reproduce the hardware-proved drivers byte for byte. **This hits the shipped app too** (every capture session ends in a DROP), so `audio_slab_ok()` turns an unpatched build into a loud message rather than a dead device |
| **ESP32 GDMA: `dma_stop()` returns before the channel stops writing (HW-027)** | **Verified PASS (HW-027)** | `evidence/20260713-hw027-dma_stop-returns-before-the-channel-stops.log` | The root cause under HW-021, and **more general than I2S**: `dma_esp32_stop()` writes `GDMA_INLINK_STOP_CHn` and returns 0, but the TRM marks that bit **(R/W/SC)** - a self-clearing command **strobe with no acknowledge** - while `GDMA_INLINK_PARK_CHn` is **(RO)** *status*. Every ESP32 DMA client that frees or reuses a buffer after a successful `dma_stop()` is exposed. **The obvious barrier is a trap and this run is what proves it:** a bounded wait on `INLINK_PARK`, measured over **890 stops, found the FSM ALREADY IDLE every single time** - it never spun once - and the DMA wrote into the freed block anyway (canary 1/8, crash at cycle 5). PARK sees "no more descriptors to fetch", not "no more bytes in flight". **What works is resetting the channel** (`GDMA_IN_RST_CHn` = "RX FSM and RX FIFO **pointer**"), and the **ablation is the load-bearing measurement: the reset ALONE, I2S unit left running, no peripheral-side stop at all -> 100 cycles, canary 0/8, slab flat.** Stopping the I2S unit also stops the writes, but that is a *timing* argument; only the reset has a *register* behind it. Upstream: zephyr #113310 / PR #113311 commit 1 of 2. Corroboration, not proof: open esp-idf **#18640**, where an Espressif engineer says "gdma_reset won't reset registers. It only reset the FIFO and FSM" - the exact scope this relies on - and proposes a GDMA reset in I2S teardown as their own fix. **Nobody at Espressif has said the channel keeps writing after `gdma_stop()`; that is ours, and it rests on the canary and the crash.** |
| **The shipping candidate: both commits, incl. the DMA chunking boundary (HW-028)** | **Verified PASS (HW-028)** | `evidence/20260713-hw028-i2s-dma-shipping-candidate-PASS.log` | Both patches together, 100 cycles, **twice**. 20 cycles with TX deliberately starved into its `tx_disable` path, 23 ending in a graceful `I2S_TRIGGER_STOP` and 12 in an `I2S_TRIGGER_DRAIN` (**both reach `I2S_STATE_STOPPING`, by different routes through TX; DROP goes straight to READY under `irq_lock` and never gets there at all**). **All 35 drained their tail block** - the harness now *asserts* on that, because an earlier version counted it, printed `drained 0` 27 times, and still printed PASSED. Canary 0 of 8, slab flat 8/8. The second run used **4096-byte blocks**, which cross `I2S_ESP32_DMA_BUFFER_MAX_SIZE` (4092) and take the RX callback's **chunking** path (4092 + 4), where `mem_block` is *indexed* rather than merely held. That image was proven to be the one that ran by reading the slab out of the ELF (`nm -S`: `_k_mem_slab_buf_rx_slab` = `0x8000` = 8 x 4096), not by trusting the build script - twice this week a mutation was reported from a build that never contained it. **Not covered: ESP32/ESP32-S2 (non-GDMA).** They get the unit stop and `i2s_hal_rx_reset_dma()` but deliberately still leak the block, behind `#if SOC_GDMA_SUPPORTED`: the AHB master and its command FIFO (`I2S_AHBM_RST`, `I2S_AHBM_FIFO_RST`) have **no HAL accessor at all**, so no stop sequence can touch them, and a slow leak beats heap corruption on silicon we cannot run. |
| Octal PSRAM (issue #13) | **Verified PASS (HW-020)** | `evidence/20260712-hw020-psram-selftest-PASS.log` (3rd physical StickS3, COM35: ROM `Found 8MB PSRAM device` / `Speed: 40MHz` / octal vendor 0x0d, 64 Mbit, good-die Pass; app `65536 B of the external heap verified (pattern, complement, address alias)`, no failure banner, device alive afterwards); earlier `evidence/20260611-hw018-psram-selftest.log` (boot: `Found 8MB PSRAM device` / `Speed: 40MHz`, then `psram: octal SPIRAM mapped; 65536 B external R/W verified OK`); CI `build (psram)` green, 8 MB `ext_dram_seg` | Gated `CONFIG_APP_PSRAM` (`overlay-psram.conf`): `CONFIG_ESP_SPIRAM`/`SPIRAM_MODE_OCT` enable the on-board 8 MB octal SPIRAM (GPIO33–37). The boot self-test (`app/src/psram.c`, run early in main to dodge the multi-threaded SMH-alloc hang zephyr#98137) allocates 64 KB from the external shared-multi-heap, R/W-verifies it on real hardware, and confirms `esp_ptr_external_ram`; the device stays alive afterwards (no hang). Mutually exclusive with Wi-Fi (`depends on !APP_WIFI` + a `BUILD_ASSERT`): PSRAM + Wi-Fi is **unsupported and unvalidated** here - we have never tested the combination, and zephyr#74246's root cause is an allocation policy (Wi-Fi buffers land in SPIRAM when the internal heap is small; the reporter recovered scanning by raising it), not a property of the silicon. Default build PSRAM-free. **2026-07-12, and this is a hole we opened ourselves:** HW-018 passed against the ORIGINAL self-test, whose pattern was `buf[i] = (uint8_t)(i ^ 0xA5)`. Truncating to a byte makes that pattern repeat every 256 bytes, so a stuck A8 would alias 0x0000 and 0x0100, both cells would hold 0xA5, and both would be *expected* to hold 0xA5: the test passed with the fault present, while its own comment claimed it "catches stuck address lines". Only A0-A7 were ever covered. The self-test was rewritten (32-bit words carrying the full index, a complement pass, and an explicit power-of-two address-alias sweep) and **re-run on silicon as HW-020 (2026-07-12): it passes.** So the row says PASS again, and it now means something it did not mean before: the data lines carry a position-dependent pattern AND its complement, and the low address lines are checked for aliasing directly rather than by a pattern that could not have seen it. Scope is unchanged and stated plainly: 64 KiB of an 8 MiB part (0.78%), not the whole device |
| ES8311: is the software reset harmful? (HW-023) | **Verified — and it OVERTURNS the HW-019 row above** | `evidence/20260712-hw023-es8311-reset-is-innocent-adc-needs-6s.log`, `evidence/20260713-hw025-es8311-vmid-fast-charge-inconclusive.log`, `evidence/20260712-hw019-reset-then-a-deaf-adc-SUPERSEDED.log` | **The claim "the software reset deafens the ADC below ~22 kHz" is WRONG. There is no sample-rate dependence and there never was.** The rate sweep only ever walked 8 kHz upward to 48 kHz, which is simultaneously increasing rate AND increasing time since the reset (~2 s per rate), so a settling effect and a frequency response were perfectly confounded, and no run of that sweep ever made could tell them apart. **Reversing the rate table separates them and there is no third outcome:** with the same reset, 48 kHz measured FIRST came back deaf (`tone_rms=1`) and 8 kHz measured LAST came back alive (`tone_rms=923`, against a no-reset baseline of 777). **What is real:** the ES8311 has no microphone-bias pin; its analog reference is three 1 µF capacitors (VMID, ADCVREF, DACVREF). The `0x00` reset clears CSM_ON, powering the analog down and discharging them; charging them back takes **~6.2 s**, measured directly at one fixed rate with one configure and one START (floor 0 until t=3.1 s, 101 at t=6.175 s, 100–120 stable after). Not the analog bias register (`0x10 = 0x1F` changes nothing), not the power-up timers (`0x0B`/`0x0C`), and not documented anywhere — no datasheet and no reference driver specifies any settling time, and Linux's own `es8311.c` says so in as many words. **Consequence:** "do not reset" survives; every reason previously given for it does not; and the driver does not need a reset — writing every register it depends on reaches the same known state **at zero settling cost** (measured on a settled part: capture floor 103 before those writes, 109 after, no transient). **Attempted fix, INCONCLUSIVE (HW-025):** VMIDSEL's documented fast-charge mode (`0x0D[1:0] = 3`, which no reference driver uses) appeared ~1 s faster, but two runs of the *same* control arm differ by ~1 s, so the effect is not distinguishable from the measurement's own variance. No claim made |
| ES8311: does a COLD power-on pay the same six seconds? (HW-026) | **Verified — and it REFUTES HW-023's explanation** | `evidence/20260713-hw026-es8311-cold-boot-is-not-deaf.log` (board **physically unplugged and replugged** — the one thing no warm reset can simulate, since the ES8311 has no reset pin) | **NO. A cold power-on does not go deaf, and the tidy "three 1 µF caps discharging" story is dead.** Measured, with `t` = ms since **power was applied**: `8000 t=2239 floor=10053 adc=alive PASS` … `48000 t=9642 floor=117 PASS` — **all nine rates pass, and the ADC is alive at the earliest moment the instrument can sample it (2.2 s)**. What a cold boot shows instead is a settling transient of a completely different shape: the floor starts ~100× too high (10053 vs a settled 81–116) and **decays** to baseline by ~7 s. **The three cases at the same t≈2–3 s:** cold boot → `floor=10053, decaying, ALIVE`; warm boot → `floor=93, flat, ALIVE`; after a `0x00` reset → `floor=0, DEAF`. Three different behaviours at the same elapsed time, so this is not an artefact of when we look. **Consequences:** (1) HW-023's *measurement* stands — the reset really does cost ~6.2 s of zero floor; (2) its *explanation* is refuted — a cold boot charges those same caps from zero and stays alive, so **root cause is open again**; (3) **"do not reset" is STRONGER**, because the reset costs something a cold boot does not — it is not the cold start brought forward, it puts the part somewhere a cold start never goes. **Open hypothesis, untested:** what the `0x00` reset and the L3B rail cut share, and a full board power-cycle does not, is that they are *partial* power-downs. **This claim was three hours old and already committed to the driver, the binding and the risk register when one physical unplug killed it** |
| ES8311: is the vendor low-rate `DAC_OSR` better than ours? (HW-030) | **Superseded by documentary evidence. The run measured the wrong quantity; the driver was changed anyway** | `evidence/20260809-hw030-es8311-dac-osr-ab-0x10-serial.log`, `evidence/20260809-hw030-es8311-dac-osr-ab-0x20-serial.log` | The A/B was run to decide whether to follow the Everest coefficient table, which uses `dac_osr` **0x20** at 8/11.025/12/16 kHz and **0x10** above, where this driver wrote **0x10** everywhere. **What the run does establish:** the part accepts `0x20` and reads it back (`regs=OK` at all seven rates with the expectation flipped), all seven PASS in both, and the measured `lrck` is **identical to the sample** in both, so `DAC_OSR` does not disturb the frame clock and `0x20` breaks nothing. **What it cannot establish, and this is the correction:** it never addressed the question. Espressif shipped `0x10`, hit audible noise, and fixed it in esp-adf commit `13c3bcff65`, titled *"audio_hal: fix the es8311 noise on 8k-16k"*, which changes exactly one column (`dac_osr`) on exactly the 8000/11025/12000/16000 rows. **The fix is about NOISE.** This sweep measured the RMS of a tone and the ADC noise floor with the amplifier OFF. Neither is DAC output noise. So the null result is not weak evidence for `0x10`; it is no evidence either way, and the earlier reading of it as "inconclusive, therefore leave the code alone" understated what the documentary record already showed. **Why the vendor evidence is decisive enough to act on:** esp-adf pins `MCLK_DIV_FRE 256`, so the noise was observed **in this exact clock tree, at these exact four rates**, which are this driver's four lowest. esp_codec_dev and ESPHome carry the fixed table; esp-bsp still carries `0x10` but its copy was added 2021-12-03, **before** the 2022-03-08 fix, so it is not an independent second opinion. Mainline Linux deliberately never writes 0x03 or 0x04 at all, which is the one real counter-precedent, and it targets the part generically rather than this codec beside an ESP32-S3. **Also corrected: the driver reasoning itself was wrong.** The comment justified one `DAC_OSR` for every rate by the 256fs ratio. Datasheet rev 8.0 section 8 defines 0x04 as bits 6:0 encoding N as **4N x fs**, a multiple of the sample rate, not a divider of the master clock, so that argument never covered it. `ADC_OSR` in the neighbouring register **is** rate-invariant and vendor-backed (0x10 in every single-speed row), so the two were wrongly bound under one claim. The ztest asserted the same falsified premise and now asserts the split instead; the app sweep table dropped 0x04 and checks it separately. **Instrument added:** the sweep gained a `dacfloor` phase, amplifier ON with digital silence on the wire, which is the only configuration in it that can see DAC output noise. It is reported, not asserted. **Not yet run on hardware.** **Outcome:** driver now writes 128*fs at and below 16 kHz and 64*fs above; ztest 72/72 on native_sim; app builds for the board. The hardware A/B of the new measurement is still owed |
| ES8311 external-MCLK sweep, on a stock Zephyr (HW-031) | **Blocked at 44.1 kHz — and not by the codec** | `evidence/20260810-hw031-es8311-external-mclk-sweep-BLOCKED.log` (3rd physical StickS3, COM35, MAC 98:88:e0:0e:dc:64) | Seven of nine rates PASS (8000 through 32000: clock registers read back, frame clock within 2 Hz, ADC alive), then the sweep stops dead in the 44100 prequeue and says nothing for the remaining 185 s. The mem-slab census names the culprit: free blocks fall 8, 7, 6, 5, 4, 3, 2, 1 across the eight rates — exactly one lost per reconfigure — so at 44100 one block is left, the two-block prequeue asks for a second, and `i2s_buf_write()` waits on it with `K_FOREVER`. That is upstream issue #113310 reproduced independently, in a scenario quite unlike the START/DROP stress that first found it: a plain reconfigure-per-rate sweep, no DROP loop. **Kept as evidence rather than discarded** — it is the control half of HW-032 |
| ES8311 external-MCLK sweep, on the patched Zephyr (HW-032) | **Verified PASS — 9/9 rates, 3/3 routes** | `evidence/20260810-hw032-es8311-external-mclk-sweep-PASS.log` (same board and app as HW-031; Zephyr build `697b32395414` with `scripts/patch_zephyr_dma_quiesce.sh` + `scripts/patch_zephyr_i2s_leak.sh` applied) | Same app, same board, same sweep, re-run on a Zephyr carrying the two i2s/dma fixes. The two images are not bit-identical and it would be wrong to say so: three route-check constants were corrected between them (`d4aa5e0e` for HW-031, `a8be0745` here). They are read only after the rate loop has finished, so they cannot reach the behaviour under test — but the mutation argument rests on the Zephyr tree, not on the app being frozen. All nine rates PASS including **44100 and 48000**, measured frame clock within 4 Hz everywhere (8000, 11025, 12000, 16000, 22050, 24001, 32002, 44103, 48004), ADC alive at every rate with a noise floor of 75-122, all three route transitions PASS on silicon, and the restore step measures 16 kHz back on the frame clock. **The slab stays flat at 8/8 free at all nine rates**, against 8..1 in HW-031. So the pair is a hardware mutation proof of the fix, not just a green run — with one boundary worth stating: what ran here is this repo's implementation of it, the two patch scripts named above, not the exact commit currently on the #113311 branch (local `d555a91` against a pushed head of `6fd59b7`). It demonstrates that the fix removes the stall; it is not a test of that branch's present contents. **What this does and does not prove** carries over unchanged from the HW-019 row: register readback proves only that the driver wrote what it meant to; the frame clock is the real measurement, timed against the kernel cycle counter, which is not derived from the I2S clock; and the acoustic loopback deliberately does not gate the verdict. It also settles the open question in `docs/issues/0007`: the external-MCLK clock path, which until now existed only in the driver and the emulator, works on silicon at every rate the driver advertises — including the six the BCLK-derived mode refuses (8000 through 24000, where 32 * Fs leaves the x8 multiplier input at or below the 1 MHz floor). Three harness expectations had to be corrected first, all of them stale in the same way (the clock manager no longer sets bit 7 because the board feeds a real MCLK; `configure()` no longer unmutes the ADC serial port, `start(RX)` does) — the silicon disagreed with the test, not with the driver |
| Upstream PR | Pending | — | After button/LCD evidence; split per `docs/07_UPSTREAM_PLAN.md` |

## Verification levels (per `CONTRIBUTING.md`)

- **scaffolded** — file exists, not built.
- **build-verified** — compiles under Zephyr 4.4 (board discovery + `west build`).
- **flash-verified** — flashes to hardware with verified hash.
- **runtime-verified** — observed running on hardware via serial/photo/video.
- **upstream-reviewed** — accepted/feedback from Zephyr maintainers.

As of 2026-05-30 (runtime-verified on hardware): board discovery, build, flash,
boot/console (HW-001), buttons G11/G12 (HW-002), BMI270 IMU (HW-004), and the
ST7789P3 LCD (HW-003) — the LCD via a new M5PM1 PMIC regulator driver. Remaining
roadmap: full M5PM1 (charger/GPIO/audio rails), IR, and ES8311 audio.
