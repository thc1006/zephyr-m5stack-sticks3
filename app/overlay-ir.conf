# App feature gate (adds src/ir.c + src/nec.c, the IR UI page, and ir_init()).
CONFIG_APP_IR=y

# LEDC PWM drives the ~38 kHz IR TX carrier (G46). IR RX (G42) is read as a
# plain GPIO edge interrupt (CONFIG_GPIO is already on in prj.conf), so no PWM
# capture driver is needed.
CONFIG_PWM=y
