name: CI

on:
  push:
  pull_request:

jobs:
  repo-integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run repository integrity checks
        run: bash verify.sh
